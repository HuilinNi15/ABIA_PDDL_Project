(define (problem RoversSuministrosPersonal) (:domain BasesMarte_extension1)

    (:objects 
        base
        almacen
        rover
        suministro
        personal
    )

    (:init
        (= (CapacidadPersonal) 2)
        (= (CapacidadSuministro) 1)
    )

    (:goal (and ))
)
